import {} from 'entcore';

export class ElementProgramme {
    texte : string;
    id_classe : string;

    constructor(texte, id_classe) {
        this.texte = texte;
        this.id_classe = id_classe;
    }
}